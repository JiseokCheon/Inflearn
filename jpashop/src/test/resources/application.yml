spring:
#  datasource:
#    url:  jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create  # 자동으로 테이블 만들어줌
#    properties:
#      hibernate:
##        show_sql: true   # System.out 에 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug  # log에 출력
    org.hibernate.type: trace # log에 parameter 타입 출력